# bd:deps-2026-09 WP-B1 — all pins are exact `==`, frozen from
# `uv pip install` resolve against the target floors in
# outputs/deps-2026-09/01-sara-adr-migration.md §1.3 (`uv pip freeze`
# output pasted in outputs/deps-2026-09/10-dave-backend-b0-b5.md).
# No floating specifiers on this branch (§1.4 pin rule).

# Web framework
fastapi==0.141.1
uvicorn[standard]==0.52.4
gunicorn==26.2.0
uvicorn-worker==0.4.0

# Database
asyncpg==0.31.0
sqlalchemy[asyncio]==2.0.52
alembic==1.19.1

# Redis & Cache
redis==8.1.0
celery==5.6.3

# Auth
# bd:deps-2026-09 iter1 (CHRIS-05) — passlib[bcrypt] removed: its
# CryptContext bcrypt backend is unconditionally broken against bcrypt
# 5.0.0 (probes the removed bcrypt.__about__ submodule; the resulting
# self-test crash is unrelated to any real password's length). Replaced
# with direct bcrypt.hashpw() calls in core/security.py — passlib's
# multi-scheme abstraction was dead weight anyway (bcrypt is the only
# scheme ever configured here).
PyJWT==2.13.0
bcrypt==5.0.0
google-auth==2.57.0
python-multipart==0.0.32

# Data
pandas==3.0.5
numpy==2.5.2
requests==2.34.2
httpx==0.28.1
yfinance==1.4.1

# Sync DB driver (required by Celery workers — asyncpg is async-only)
psycopg2-binary==2.9.10

# Validation
pydantic==2.13.5
pydantic-settings==2.15.0
email-validator==2.2.0

# Logging
structlog==26.1.0

# Telegram
python-telegram-bot==22.8

# Feed parsing
feedparser==6.0.11

# Thai mutual fund NAV — fetched directly from SEC Open Data API (no pythainav dep)
